//To handle this unexpected behavior, we should always type check the inputs before performing the operation.function foo(a, b) {  if (typeof a === 'number' && typeof b === 'number') {    return a + b;  } else {    return String(a) + String(b);  }}console.log(foo(2, 3)); // Expected Output: 5console.log(foo(2, "3")); // Expected Output: 23console.log(foo("2", 3)); // Expected Output: 23console.log(foo("2", "3")); // Expected Output: 23